Nocion importante sobre las bases, el formato de los datos. Empemos con los textos:

a la hora de genera las computadoras se establecio como codificar caracteres. surge de la forma de decodificar señales por cable en los teelgafos. se usbana bits en e6 posibliades, por lo que teniamos solo 64 opciones, donde ya 36 estaban ocupadas por las letras, y unos cauntos mas por simbolos de puntuacion. con eso se empienzan a mapear numeros a caracteres. con esto arranca **ASCII**.
**ASCII** sera un sistema de 7 bits donde hay mapeo de numeros a caracteres. Como sobraban caracteres para el mapeo se agregaron codifiaciones extra para ciertos simbolos especiales.
El problema es que para el resto del mundo faltaban cosas, por lo que se empieza a reservar ese espacio para generar mas entradas para el resto del mundo.

Para mejorar la cantidad de caraters primero se genera el modelo **ANSI**, donde se agrego caracteres suficientes para la iindesutrai aglosajona. esta pposia 256 entradas. Se empiezan a genera ditsintas mpas de codigo que seran distintas tablas de numero a caracteres. Esto impacto en las bases de datos, tal que habia que elegir que mapeo se ultilizaba. El problema surege al mezclar juegos de caracteres. Si por ejemplo yo elijo un mapeo distinto al que se contruyo la base, la iformacion se muestra de forma extraña. 

para solucionar esto, se decidio listar todos los caracteres de todos los lenagujes que exites. Asi se genero el consorcio **UNICODE**. Buscaron genera una codificacion general. Hubo caractres que se eligieron no agregar, generando dos posturas sobre esto. El unicode tambien tendra los emojis. Este sistema intento ayudar al **ANCII**. El unicode no es un formato, solo la asignacion. 

para la codifiacion, en **ANSI** teniamos un byte por letra. En **UNICODE** teniamos UTF8, UTF16 y UTF32. Con 3 bytes se podiran representar todos lo unicos, el probelma de 3 bytes por caracter es que es muy pesado. Por esto surge la version de UTF, teniendo ditsintas versiones. El 8 para el **ASCII** es compatible, todo el **ASCII** se podra representar en **UTF8**. la 16 donde un caracteres se puede dividir en 2 de 16, lo mismo para 32.

El primer bit en UTF88 va indicar si es o no muticaracters, si es 0 es que no, si es 1 es que si. A partir de 128 en adelante,  van a tener 2 3 o 4 bits. esto se representa con un numero unario. esto indica cuantos bytes tiene la representacion. El prefijo 10 signica caracter de continuacion. 
para los 16 bits se les ocurrio uan idea, si el numero enra en 16 bits su represnetacion es todo eso, ni no entra en uno se representa con 2 numero dde 16 bits. Unciode tenra un rango de numero que no usa, por lo tanto va aa mapera todos los carcates entre 0 y un limite. La idea es separa todos los que necito, para aplicar el producto cartesiano de lo que me queda representar. A esos caracteres se los denomina subrogdaos, con dos subrogados se amra un caracter nuevo. 
otras cosa que impacta en la codifiacion es big endian o little endian. para establcer que tipico de codifcaion se usa el FEFF, en base a que modelo  se usa uno distinto, cambia entre 8,16 y 32. es un campo obligatio pero optativo, tal que si lo puedo detectar lo puedo genera de en el archivo. 

otra cuestion con relacion a los textos es collation, habla de cultura que tiene el texto o sistema a la pesona que esta dirigida, por ejmplo, esto es lago que voy usar en españa, o esto es algo que voy usar en el mundo. Esto se define cuando se genera la base de datos. Esto afecta a como se van a realiar la comparaciones, como contaplamos los string para establcer la igualdad entre ellos. basicamente tengo problema con estos cuando intento hacer la misma comparacion entre frontend, backend y la base de datos. se  necesario que estos 3 tega la misma collation.

otro porblema de las decodificaiones es el tema del tiempo.hay probelmas con los formatos a nivel mundial, hay diferencias en como se lee. Tendra otro formato ISO, a veces reconcoe los froamtos humanos pero hay un estadnar. El formato sera año, mes dia. Con ester formato lograron que si vez el 4 cifras al toque ya sabes que es un año, Otra ventaja es que es ordanable en si mismo, ordena por byte. Esto agregara forma para semana y para semestre. Otra cocsa importen sera el tme zone, que definira la hora. Los tiempos son como vectores. Te permite calcular diferencias por medio de esta repsetacion de vectores. Tambien permite genera intervalos. La udracion tambein se pdra, teniendo una P adelnte y numero que indica letra para si es año y el numero que sera la longitud. La duracion se puede espeficicar en la cantidad de cosas que queramos. 